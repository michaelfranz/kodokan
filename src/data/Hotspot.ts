export default class Hotspot {
    public term: string
    public x: number
    public y: number

    constructor(term: string, x: number, y: number) {
        this.term = term
        this.x = x
        this.y = y
    }
}
